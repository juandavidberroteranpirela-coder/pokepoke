services:
  - type: web
    name: pokepoke
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python launcher.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
